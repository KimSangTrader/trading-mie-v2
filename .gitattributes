# 모든 파일: auto 모드 (자동 변환)
* text=auto

# Python 파일: LF 유지
*.py text eol=lf
*.sh text eol=lf

# Windows 배치 파일: CRLF 유지
*.bat text eol=crlf
*.cmd text eol=crlf

# 바이너리 파일: 변환 안 함
*.db binary
*.sqlite binary
*.pyc binary